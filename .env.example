# MongoDB Database Connection
MONGODB_URI=mongodb://localhost:27017/matri-shakti-solar

# Admin Initial Credentials (seeded into MongoDB on first startup if no admin exists)
ADMIN_EMAIL=admin@matrishakti.com
ADMIN_PASSWORD=Admin@123456

# JWT Session Security Secret (minimum 32 characters)
SESSION_SECRET=matri-shakti-solar-portal-session-secret-key-2026-prod

# API URL (Leave blank to use internal same-origin /api proxy)
VITE_API_URL=
